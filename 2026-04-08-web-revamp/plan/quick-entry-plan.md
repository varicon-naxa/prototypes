The new architecture:
The existing codebase already has a solid structure — sidebar, header, tab nav, tile strip, and section pages loaded via data-include. Rather than rewriting everything from scratch, Lets restructure and rebuild whenever required.




Module :

Weather (auto-fetch display + observed form)
Notes (multi-section + rich text)
Photos (grid + upload + tags)
Plant & Equipment (operating + stand down)
Labour (group by supplier/resource)
Materials (inline table + task matrix)
Miscellaneous (simple resource table)
Forms (assigned + submissions)
Timecard (matrix + normal view)
Delivery Dockets
